IF(FREETYPE_FOUND)
LIST(APPEND TESTS_FILES
	bug00615
)

IF (NOT MINGW)
IF(PNG_FOUND)
LIST(APPEND TESTS_FILES
	gdimagestringft_bbox
)
ENDIF(PNG_FOUND)
ENDIF(NOT MINGW)

ENDIF(FREETYPE_FOUND)

ADD_GD_TESTS()
